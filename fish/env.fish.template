# Environment variables for AI/ML tools.
# Source this from config.fish: source ~/.config/fish/env.fish
#
# NEVER hardcode tokens here. Use one of:
#   huggingface-cli login          (writes ~/.cache/huggingface/token)
#   set -gx HF_TOKEN (cat ~/.hf_token)   (read from a file outside the repo)
#   a secrets manager

# HuggingFace
set -gx HF_HOME ~/.cache/huggingface
# set -gx HF_HUB_CACHE ~/.cache/huggingface/hub   # default: $HF_HOME/hub
# set -gx HF_DATASETS_CACHE ~/.cache/huggingface/datasets

# GitHub — token is managed by `gh auth login`, not exported here
# set -gx GITHUB_TOKEN (cat ~/.github_token)

# OpenAI / Codex — use `codex auth` or load from a secrets file
# set -gx OPENAI_API_KEY (cat ~/.openai_token)

# Anthropic / Claude — use `claude login` or load from a secrets file
# set -gx ANTHROPIC_API_KEY (cat ~/.anthropic_token)
